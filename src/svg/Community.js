export default function CommunityIcon({ className }) {
  return (
    <img
      src="/assets/img/caixa-de-entrega.png"
      alt="Uber Icon"
      className={`scale-75 border-[100%] border-white rounded-full ${className}`} // Adicionando borda arredondada
    />
  );
}
