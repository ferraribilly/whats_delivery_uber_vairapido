export default function FilterIcon({ className }) {
  return (
     <img
      src="/assets/img/UberX.png"
      alt="Uber Icon"
      className={`scale-[200%] border-[100%] border-white rounded-full ${className}`} // Adicionando borda arredondada
    />
  );
}
