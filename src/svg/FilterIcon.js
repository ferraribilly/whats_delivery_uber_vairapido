export default function FilterIcon({ className }) {
  return (
     <img
      src="/assets/img/car_13260.png"
      alt="Uber Icon"
      className={`scale-125 border-[100%] border-white rounded-full ${className}`} // Adicionando borda arredondada
    />
  );
}
